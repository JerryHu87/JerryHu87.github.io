#include <bits/stdc++.h>

using namespace std;

int main() {
    cin.sync_with_stdio(0);cin.tie(0);
    int n; cin >> n;string a;
    for(int i = 0;i<n;i++){
        cin >> a;
        cout << a.size() << "\n";
    }
    return 0;
}
